#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int r;
    cin >> r;
    cout << fixed;
    double pi = 2 * acos(0.0);
    cout << pi * r * r << "\n";
    cout << r * r * 2;
    return 0;
}
